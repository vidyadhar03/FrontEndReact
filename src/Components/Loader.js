import React from 'react';
import './Loader.css'; // You can define your loader styles in this CSS file

const Loader = () => {
  return (
    <div className="loader">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
